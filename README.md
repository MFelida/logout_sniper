# Logout Sniper
## Don't let them take those sweet coalition points

To install clone the repo and run ```INSTALL.sh``` or run
```
bash <(curl -fsSL https://raw.githubusercontent.com/MFelida/logout_sniper/refs/heads/master/INSTALL.sh | tee "${HOME}/.local/share/logout_sniper.sh")
```
